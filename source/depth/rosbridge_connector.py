# from .depth_perceptor import DepthPerceptor
# import roslibpy

# #Configure connection to ros 
# RosClient = roslibpy.Ros(host="172.0.0.1",port=9090)
# RosClient.run()
#  #Configure Ros Topic
# talker = roslibpy.Topic(RosClient, '/Object-Detection', 'std_msgs/String')
# talker.publish(roslibpy.Message)
